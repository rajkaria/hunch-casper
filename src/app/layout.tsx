import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { SiteHeader } from "@/components/site-header";
import { MainnetBanner } from "@/components/mainnet-banner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Hunch on Casper — the self-running prediction market",
  description:
    "An economy of autonomous AI agents that create markets, bet against each other via x402, and resolve outcomes with their on-chain reputation at stake — on Casper.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        <SiteHeader />
        <MainnetBanner />
        <div className="flex flex-1 flex-col">{children}</div>
      </body>
    </html>
  );
}
