# Hunch on Casper — environment. All optional: sensible public defaults live in
# src/config/network.ts. Set these on the Vercel project as contracts land (S1+).

# Default network the UI opens on ("testnet" | "mainnet")
NEXT_PUBLIC_DEFAULT_NETWORK=testnet

# --- Testnet ---
NEXT_PUBLIC_CASPER_TESTNET_RPC=https://node.testnet.casper.network/rpc
NEXT_PUBLIC_CSPR_CLOUD_TESTNET=https://api.testnet.cspr.cloud
NEXT_PUBLIC_CASPER_TESTNET_EXPLORER=https://testnet.cspr.live
NEXT_PUBLIC_TESTNET_MARKET_FACTORY=
NEXT_PUBLIC_TESTNET_ORACLE_REGISTRY=
NEXT_PUBLIC_TESTNET_VAULT=
# S16 singleton HunchVault v2 (contracts/src/hunch_vault.rs). When set, slugs NOT in
# NEXT_PUBLIC_TESTNET_MARKET_ADDRS route here with the slug as the market_id runtime arg
# (cheap create_market entries — no per-market installs). Legacy per-market packages in
# the map keep winning, so the five pre-S16 deploys stay routable unchanged.
NEXT_PUBLIC_TESTNET_VAULT_V2=
# Per-market ParimutuelMarket package hashes from the full-catalogue deploy (slug → hash-<64hex>,
# JSON). Bets/resolves route to the market's own contract; unmapped slugs fall back to _VAULT.
# e.g. {"the-flip":"hash-…","cspr-above-5c-aug1":"hash-…"}
NEXT_PUBLIC_TESTNET_MARKET_ADDRS=

# --- Mainnet ---
NEXT_PUBLIC_CASPER_MAINNET_RPC=https://node.mainnet.casper.network/rpc
NEXT_PUBLIC_CSPR_CLOUD_MAINNET=https://api.cspr.cloud
NEXT_PUBLIC_CASPER_MAINNET_EXPLORER=https://cspr.live
NEXT_PUBLIC_MAINNET_MARKET_FACTORY=
NEXT_PUBLIC_MAINNET_ORACLE_REGISTRY=
NEXT_PUBLIC_MAINNET_VAULT=
NEXT_PUBLIC_MAINNET_VAULT_V2=
NEXT_PUBLIC_MAINNET_MARKET_ADDRS=

# CSPR.cloud API key (server-side). With a key, Genesis reads a LIVE validator count from
# CSPR.cloud; without one it falls back to the network's public node RPC (latest block height),
# then to the deterministic demo rotation. Set CASPER_LIVE_SIGNALS=false to force the rotation.
CSPR_CLOUD_API_KEY=
CASPER_LIVE_SIGNALS=

# On-chain proof receipts — real tx hashes minted by the ops deploy, rendered as cspr.live links
# on the landing page ("Live on Casper") + docs. JSON array of {label, hash, network}, e.g.
# [{"label":"First real bet (payable proxy)","hash":"<64 hex>","network":"testnet"}]
NEXT_PUBLIC_ONCHAIN_RECEIPTS=

# --- Real Casper chain adapter (S2) ---
# Default is the mock adapter (deterministic, credential-free) so CI + the demo need no secrets.
# Set to "real" to sign + submit live Casper transactions via casper-js-sdk (server-only).
CASPER_CHAIN_MODE=mock
# Ed25519 secret key that signs + funds bets — PEM contents (casper-client keygen secret_key.pem)
# or a 32-byte hex seed. Server-only; NEVER prefix NEXT_PUBLIC_.
CASPER_BETTOR_KEY=
# HMAC key binding a prepared wallet-signed bet to the transaction hash it will carry on chain
# (docs/OPS.md §3c). Optional: falls back to CRON_SECRET, then CASPER_BETTOR_KEY. Server-only.
BET_TICKET_SECRET=
# Ed25519 secret key for the oracle's resolve. Falls back to CASPER_BETTOR_KEY when unset.
# ⚠️ Must sign as the account the markets name as `config.oracle` — the vault checks
# `caller == config.oracle` on resolve, so a fresh "oracle" keypair that no market names
# reverts NotOracle on every resolution. Catalogue markets name the DEPLOYER.
CASPER_ORACLE_KEY=
# Odra's proxy_caller_with_return.wasm (needed to attach CSPR to a payable bet) ships in-repo
# at src/adapters/casper/resources/. Set this ONLY to override that bundled copy.
CASPER_PROXY_WASM_PATH=
# Operator resolve route (/api/chain/resolve). MOCK mode: on unless set to "false".
# REAL mode: FAIL-CLOSED — the route stays disabled unless this is exactly "true" AND a
# matching CASPER_RESOLVE_OPERATOR_TOKEN is presented (resolution is a money-path authority;
# the autonomous Arbiter replaces this operator gate in S6+).
CASPER_ENABLE_RESOLVE_ROUTE=true
# Shared secret required (real mode only) in the `x-operator-token` header to POST a resolve.
CASPER_RESOLVE_OPERATOR_TOKEN=
# Real-mode agent x402 rail. In real chain mode the rail (`/api/agent/v1/bet`, MCP place_bet,
# the Prophet fleet) is OFF unless ONE of the two below is set. Mock mode (default) is unaffected.
#
# Operator treasury account (Casper public key hex or account-hash-…) agent x402 payments must
# land in. Setting it wires the REAL transfer-verifying PaymentPort in real mode: a payment proof
# is accepted only if its deployHash is a SUCCESSFUL on-chain native CSPR transfer from the payer
# to this account of at least the quoted amount (trustless verification, node-RPC read, no key).
# Note: the internal Prophet fleet fabricates mock proofs, so with this set the fleet cannot bet
# in real mode unless it funds genuine transfers — external agents paying real CSPR are the point.
CASPER_X402_PAYTO=
# Weaker legacy opt-in: keep the MOCK PaymentPort in real mode (proofs verified by nonce-match
# only, NOT an on-chain-transfer check). Set to exactly "true" to accept that trade-off; prefer
# CASPER_X402_PAYTO above.
CASPER_REAL_AGENT_X402=

# --- Prophet fleet wallets (S17) ---
# One secret, one Ed25519 identity per agent: each agent's key is HMAC-SHA256(seed,
# "hunch-fleet-v1:<agentId>"). Derivation is deterministic, so the same agent keeps the same
# funded address across restarts, redeploys and instances — a drifting address would strand the
# balance. Set this (in real mode) and the fleet pays its own x402 bills with genuine CSPR
# transfers that the transfer-verifying PaymentPort accepts. Unset ⇒ the fleet keeps the mock
# wallet, its proofs stay obviously fabricated, and real-mode verification rejects them (safe,
# visible failure). A shared key across agents is deliberately NOT supported: it would collapse
# every agent's track record into one indistinguishable on-chain identity.
# Fund the derived accounts: GET /api/health lists each agent's account + accountHash + balance;
# `contracts_catalogue fleet-fund <cspr-each> <account-hash,…>` tops them up from the deployer.
CASPER_FLEET_SEED=
# Optional per-agent override, e.g. CASPER_PROPHET_KEY_AGENT_MOMENTUM (PEM or 32-byte hex).
# Takes precedence over derivation — for moving one agent's key into separate custody.
#CASPER_PROPHET_KEY_AGENT_MOMENTUM=

# How many Prophets bet per tick. Mock mode runs the whole fleet (free, and the rivalry inside one
# round IS the demo). Real mode defaults to 1 and rotates which agent acts: four bets per tick at
# the 10-minute cadence is 576 real transactions a day — ~1,340 CSPR/day of escrow gas alone, well
# past a faucet-funded deployer. Raise only if you have funded for it. See docs/OPS.md §5.
CASPER_PROPHETS_PER_TICK=

# --- Runtime market creation (S17) ---
# The account bound as the oracle of every Genesis-created market — the ONLY address that can
# resolve it, i.e. that decides who gets paid. REQUIRED for real-mode creation: the vault refuses a
# creator who names themselves, and there is no safe default, so without this Genesis creates
# off-chain and labels the action `simulated` rather than inventing an address.
CASPER_ORACLE_ACCOUNT=
# Creation bond attached per market, in motes (default 1000000000 = 1 CSPR). Held by the vault and
# refunded to the creator at clean settlement.
CASPER_CREATION_BOND_MOTES=
# Divisor applied to a market's catalogue seed pools when the house stakes opening liquidity in
# real mode (default 500 → ~1 CSPR a side). Ratio-preserving: the seed pools are the opening odds.
CASPER_HOUSE_SEED_DIVISOR=
# --- Human wallet (S18) ---
# CSPR.click app id — the ONLY variable you need to set for a real wallet. With it set, the app
# emits the SDK bootstrap and loads the loader (see NEXT_PUBLIC_CSPR_CLICK_BUNDLE_URL below, which
# now has a working default), the Connect button signs with a real wallet, and the `demo` pill
# retires. Unset, no third-party script is served at all and every visitor gets the clearly-
# labelled demo account rather than a button that does nothing.
# CSPR.click issues a DIFFERENT app id per network, so prefer the two network-specific vars; the
# shared one below is the fallback when only one network is configured. The id in force is the one
# for NEXT_PUBLIC_DEFAULT_NETWORK, whose chainName the SDK signs against (src/config/csprclick.ts).
# An app id is a public identifier — it ships in the browser bundle. It is not a secret.
# It MUST be an id the CSPR.click console actually issued. A made-up value is worse than none:
# the SDK fetches accounts.cspr.click/api/application/<id>.json, gets 401 "wrong application id",
# and crashes unhandled — no event, no picker, a Connect that can never work. The app probes the
# same endpoint on load and falls back to the labelled demo wallet (with the reason in the bet
# panel and a FAIL on /api/health) when the id is rejected. Verify yours before deploying:
#   curl -si -H "Origin: https://<your-domain>" \
#     https://accounts.cspr.click/api/application/<APP_ID>.json | head -1   # want HTTP 200
# In the CSPR.click console, whitelist the REST/RPC endpoints the wallet widget uses: /accounts/**
# and /rates/**, plus account_put_deploy, account_put_transaction, info_get_deploy,
# info_get_transaction and query_balance.
NEXT_PUBLIC_TESTNET_CSPR_CLICK_APP_ID=
NEXT_PUBLIC_MAINNET_CSPR_CLICK_APP_ID=
NEXT_PUBLIC_CSPR_CLICK_APP_ID=
# WalletConnect Cloud project id — what makes the QR pairing route (desktop with no extension,
# or a phone wallet) actually work. The SDK's WalletConnect provider throws "WalletConnect
# settings not present" unless init() carries `walletConnect: {projectId}`; this var is that
# project id. Mint one free at https://cloud.reown.com (formerly cloud.walletconnect.com) — it is
# public, like the app id. UNSET, the app stops OFFERING WalletConnect: a visitor with no
# extension gets the honest install prompt instead of "Could not establish a connection with the
# provider" from a QR dialog that can never start.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
# The script that installs window.csprclick. Defaults to the upstream loader
# https://cdn.cspr.click/latest/csprclick-sdk-2.1.js — the path the official UI bundle builds for
# itself, verified 2026-07-25 (HTTP 200, 1.4 MB, text/javascript). Leave BLANK unless you are
# pinning a version or self-hosting; an earlier revision hardcoded a jsDelivr npm URL that 404'd,
# hence the paranoia. Set it to a bogus value and /api/health still reports `wallet` honestly.
NEXT_PUBLIC_CSPR_CLICK_BUNDLE_URL=
# How the wallet UI is presented: "popup" (default) or "iframe". NOT the network — that is
# chainName, derived from NEXT_PUBLIC_DEFAULT_NETWORK. Leave blank unless you also mount
# CSPR.click's React navbar: in "iframe" mode the SDK draws no account picker itself, it emits an
# event for that component library to render, so Connect silently does nothing without it.
NEXT_PUBLIC_CSPR_CLICK_CONTENT_MODE=

# Show the operator "Oracle resolve" control on market detail pages. OFF by default (the autonomous
# Arbiter resolves); set to "true" only to demo the S2 thin-slice resolve path.
NEXT_PUBLIC_SHOW_DEMO_RESOLVE=

# --- Economy heartbeat ---
# Shared secret for the scheduled tick (/api/agent/tick). The credential-free mock demo accepts
# unauthenticated ticks; in REAL mode the tick moves the money path, so it is gated. Set the SAME
# value here and as the GitHub repo secret CRON_SECRET used by .github/workflows/economy.yml —
# real mode without it means every scheduled tick 401s and the economy stops advancing.
# TICK_CRON_SECRET is accepted as an alias. `GET /api/health` reports whether one is configured.
CRON_SECRET=
# Ceiling on Genesis-created markets from the open demo trigger (abuse guard). Default 12.
GENESIS_MAX_CREATED=

# LLM provider (agent narration only — never the money path). AI Gateway model string.
LLM_MODEL=anthropic/claude-sonnet-5
LLM_API_KEY=

# --- Distribution: chat bots + embeds (docs/OPS.md §9) ---
# The Telegram/X bots and the embed/oEmbed endpoints run WITHOUT any of this — in the default
# state a webhook is parsed and answered but the reply is RECORDED, never posted (decision D2).
# Outbound posting is gated behind this master switch; leave it unset to keep the bots silent.
HUNCH_BOTS_LIVE=
# Telegram: @BotFather token + a shared secret echoed on each webhook (route rejects a mismatch).
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
# X (Twitter): a bearer token authorised to create reply tweets + an optional inbound webhook secret.
X_BOT_BEARER_TOKEN=
X_WEBHOOK_SECRET=
# Optional override for the base URL used in embed/oEmbed/bot links. Defaults to the deployment's
# own domain (Vercel) or https://casper.playhunch.xyz.
NEXT_PUBLIC_SITE_URL=

# --- Economy-state persistence (Upstash Redis REST / Vercel KV) ---
# Unset (the default — local dev, CI, tests): the agent economy lives in per-instance memory and
# the deterministic demo seed papers over cold starts, so a judge's bet can vanish when the next
# request lands on a different serverless instance. Set EITHER pair below and every economy
# mutation (bets, settlements, Genesis-created markets, the activity feed, oracle reputation) is
# snapshotted to a single KV key (hunch:economy:v1) and hydrated once per instance on cold start —
# judges' bets and the boards persist across instances and deploys, and a hydrated instance never
# re-seeds demo history on top. Writes are debounced/coalesced and last-write-wins across
# instances (fine: the cron tick is effectively the single writer; the chain stays the source of
# truth for money). KV downtime is harmless — 3s timeout, then the app continues in-memory.
# Vercel KV / Upstash-via-Vercel-marketplace variable names (these win when both pairs are set):
KV_REST_API_URL=
KV_REST_API_TOKEN=
# ...or the plain Upstash Redis REST names:
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Agent League season prize pool, in motes. Unset = no prize declared (the UI says so plainly).
CASPER_LEAGUE_PRIZE_MOTES=
